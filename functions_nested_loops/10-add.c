#include <stdio.h>
#include "main.h"
/**
 * add - add 2 numbers
 * @a, @b character added together
 * Return: int
 */
int add(int a, int b)
{
    int res;

	for (alpha = 'a' ; alpha <= 'z' ; alpha++)
	{
		_putchar(alpha);
	}
	_putchar('\n');
}
