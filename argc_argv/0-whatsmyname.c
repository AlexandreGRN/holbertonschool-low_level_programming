#include <stdio.h>
#include <stdlib.h>

/**
 * main - print its own name
 * @argc: number of arguments
 * @argv: vector of arguments
 * Return: 0
 */

int main(int argc, char **argv)
{
	argc = argc;
	printf("%s", argv[0]);
	printf("\n");
	return (0);
}
